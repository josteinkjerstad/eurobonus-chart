---
export const prerender = false
import { CsvUpload } from "../components/upload/CsvUpload";
import { DeleteTransactionsButton } from "../components/upload/DeleteTransactionsButton";
import Layout from "../layouts/Layout.astro";
import { createServerClient, parseCookieHeader } from "@supabase/ssr";
import {Content as UploadContent} from "../content/upload.md";
import { getLocalUserId } from "../utils/localUser";

const { cookies, request } = Astro;
const supabase = createServerClient(
    import.meta.env.SUPABASE_URL,
    import.meta.env.SUPABASE_KEY,
    {
      cookies: {
        getAll() {
          return parseCookieHeader(request.headers.get("Cookie") ?? "");
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value, options }) =>
            cookies.set(name, value, options)
          );
        },
      },
    }
);

const userId = (import.meta.env.MODE === 'development') ? getLocalUserId() : (await supabase.auth.getUser()).data.user?.id;

if (!userId) {
  return redirect("/signin");
}
---

<Layout title="Upload Transactions">
  <UploadContent/>
  <CsvUpload client:load/>
  <DeleteTransactionsButton client:load/>
</Layout>